services:
  - type: web
    name: a1-fence
    runtime: node
    rootDir: .
    buildCommand: npm install
    startCommand: node server/server.js
    envVars:
      - key: NODE_VERSION
        value: 22
